{% extends 'base.html.twig' %}
{% block title %}
	Modifier une figure
{% endblock %}

{% block body %}

	{{ form_start(form) }}

	{{ form_row(form.title)}}
	{{ form_row(form.content)}}
	{{ form_row(form.groupeTrick)}}
	{{ form_row(form.image)}}
	{{ form_row(form.video)}}

	<!-- Submit Button-->
	<div class="d-grid">
		<button class="btn btn-primary btn-lg" id="submitButton" type="submit">Enregistrer
		</button>
	</div>
	{{ form_end(form) }}


{% endblock %}
